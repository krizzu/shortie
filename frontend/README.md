# Shortie website

This project hosts both single pages used as template from API and Admin dashboard.

## Building pages

- install dependencies via `yarn`
- run pages build via `yarn build:pages`
- `dist-pages` is where production build is at


## Local development

- copy .env-example to .env and adjust variables